---
# ansible_ssh_common_args: "-o PasswordAuthentication=yes -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -o ProxyCommand='ssh -p 22 -J {{ ansible_ssh_user }}@{{ groups.servers[0] }}'"

controller: "{{ groups.servers[0] }}"
